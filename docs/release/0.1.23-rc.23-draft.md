# MFA-Forge 0.1.23-rc.23

`0.1.23-rc.23` is the local baseline cut after moving the startup update trigger to GUI process startup / loader, before unlock.

## Scope

- release line: `RC23`
- numeric version: `0.1.23`
- intended MSI asset: `MFA-Forge-RC23-x64.msi`
- publication status: local-only baseline for `RC23 -> RC24`

## Updater correction in this line

- the automatic update check now starts on GUI process startup, before unlock
- it no longer waits for `Screen::Main`
- the launcher still resolves the latest published prerelease RC, verifies checksum, and hands off to MSI

## Planned validation

- install local `RC23` on the host as the exact baseline
- publish `RC24` as the next prerelease
- validate `RC23 installed -> open GUI -> launcher discovers RC24 -> checksum verification -> MSI handoff -> installed binaries become 0.1.24`

## Local evidence captured

- local MSI path: `target/rc/MFA-Forge-RC23-x64.msi`
- local MSI SHA256: `3325A00B3A5B6204895B3A779BD77BAD1317E60F5D5445A0B76DDA463ABB188A`
- installed baseline result after `msiexec`: `C:/Users/wnune/AppData/Local/MFA-Forge/bin/`
  - `mfa-forge-gui.exe` -> `0.1.23`
  - `mfa-forge-agent.exe` -> `0.1.23`
  - `mfa-forge-mcp.exe` -> `0.1.23`
  - `mfa-forge-launcher.exe` -> `0.1.23`
